// LocalConfig.example.swift
// Copy this file, rename the copy to LocalConfig.swift,
// and replace YOUR_MAC_LOCAL_IP with your Mac's local IP address.

enum LocalConfig {
    static let serverIP = "YOUR_MAC_LOCAL_IP"
    static let serverPort = "5001"
}
